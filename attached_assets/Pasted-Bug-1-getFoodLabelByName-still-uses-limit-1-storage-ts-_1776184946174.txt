Bug 1 — getFoodLabelByName still uses .limit(1) (storage.ts)
This is the only one that survives into the new code. The useCount-aware logic was agreed but not applied:

typescript
// ❌ Current — arbitrary first row, ignores useCount entirely
const [row] = await db.select().from(foodLabels)
  .where(nameMatch)
  .limit(1);
return row ?? null;
For any food with multiple combos (e.g., bbq_pork_rice__medium__char_siu_sauce vs bbq_pork_rice__small), the label endpoint pre-fills a random default forever. Fix:

typescript
// ✅ Replace the entire getFoodLabelByName body
async getFoodLabelByName(name: string): Promise<FoodLabel | null> {
  const normalised = name.trim().toLowerCase();
  if (!normalised) return null;

  const rows = await db.select().from(foodLabels).where(
    or(
      sql`lower(${foodLabels.foodNameEn}) = ${normalised}`,
      sql`lower(${foodLabels.foodNameZhHant}) = ${normalised}`,
      sql`lower(${foodLabels.foodNameYue}) = ${normalised}`,
    )
  );

  if (rows.length === 0) return null;

  const maxCount = Math.max(...rows.map(r => r.useCount));
  if (maxCount === 0) return rows[Math.floor(Math.random() * rows.length)];
  return rows.reduce((best, r) => r.useCount > best.useCount ? r : best);
}
Bug 2 — Seed insert body not visible, but PORTION_MAP may be unused (seed-food-db.ts)
PORTION_MAP is defined but you need to confirm the actual insert loop (which was truncated in your paste) uses it. The insert must have:

typescript
// ✅ Both of these must appear in the insert loop
internalId: buildFullComboId(
  row.foodId,
  PORTION_MAP[row.portionEn],          // "Medium" → "medium"
  parseSauceIds(row.saucesEn),
  parseToppingIds(row.toppingsEn)
),
defaultPortionId: PORTION_MAP[row.portionEn],  // NOT row.portionEn directly
useCount: 0,
If the insert uses row.portionEn directly anywhere, Bug B is still alive.

Bug 3 — type InsertFoodLabel unused import in routes.ts (minor)
typescript
// Line 3a import includes InsertFoodLabel but it's never used as a type annotation
// in routes.ts — saveFoodLabel receives an object literal, TypeScript infers it
import { ..., type FoodLabel, type InsertFoodLabel } from "@shared/schema";
//                              ^^^^^^^^^^^^^^^^^ unused, causes a TS warning
Either remove it, or explicitly type the object: await storage.saveFoodLabel({ ... } as InsertFoodLabel).

Everything else looks correct
Item	Status
newInternalId encodes full combo (name + portion + sauces + toppings)	✅ Fixed
getFoodLabelByCombo increments useCount on exact match	✅ Fixed
saveFoodLabel via storage layer, no direct db in routes	✅ Fixed (Bug D)
adviceCacheKey = foodLabel?.internalId ?? newInternalId	✅ Correct
needsTags = !foodLabel — only fires for genuinely unknown combos	✅ Correct
useCount: 0 on all new Claude-generated rows	✅ Correct
Paste the seed insert loop if you want me to verify Bug 2 fully.

Prepared using Claude Sonnet 4.6 Thinking
Follow-ups

How to implement the useCount logic in storage.ts for other tables

Refactoring parseSauceIds to handle empty or malformed sauce strings

Best practices for mapping internal IDs during database seeding

Alternative strategies for generating unique combo keys in schema.ts

Handling edge cases for normalized food names in query lookups
