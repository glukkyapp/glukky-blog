| Likely source                  | Why it fits the screenshot                                                                                   | What to inspect                                                                                          |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| Desktop-only page wrapper      | The teal appears only outside the narrow mobile app frame                                                    | A wrapper such as .app-shell, .preview, #root, or <main> with background: #0D7E8F at desktop breakpoints |
| html or body background        | The inner mobile container is cream but does not occupy the whole document width                             | Computed background-color on html and body                                                               |
| Replit preview/iframe host     | The direct app preview may be correct, while Replit’s embedded presentation adds the teal outside it         | The iframe’s parent document and any Replit-specific preview wrapper                                     |
| Old responsive CSS rule        | Task #878 may have updated default/mobile styles but a min-width media query restores teal on larger screens | Search CSS for @media (min-width, background, #0D7E8F, and older blue token variables                    |
| Phone-size root container      | A fixed width such as max-width: 390px, width: 100vw, or max-width: 430px sits inside an outer teal surface  | The element directly surrounding the cream phone viewport                                                |
| Cached generated preview       | The code may be correct but the workspace canvas still loads an older bundle or preview URL                  | Hard refresh, restart dev server, inspect bundle/network URLs, compare timestamp or deployed build hash  |
| iframe defaults / transparency | An iframe may be transparent and reveal the teal background of its parent                                    | Computed background of the iframe document and the iframe host element                                   |