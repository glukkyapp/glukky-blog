<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <title>Onboarding Splash</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #cfe3ef;
      --surface: #ffffff;
      --text: #111827;
      --muted: #6b7280;
      --accent: #ff6b63;
      --shadow: 0 12px 30px rgba(17, 24, 39, 0.16);
      --radius-shell: 36px;
    }

    * { box-sizing: border-box; }
    html, body { height: 100%; margin: 0; }
    body {
      font-family: 'Inter', system-ui, sans-serif;
      background: var(--bg);
      color: var(--text);
      overflow: hidden;
    }

    /* ── Stage ── */
    .stage {
      min-height: 100dvh;
      display: grid;
      place-items: center;
      padding: 24px;
    }

    /* ── Three-phone gallery ── */
    .gallery {
      width: min(1180px, 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: clamp(18px, 4vw, 72px);
    }

    /* ── Phone shell ── */
    .phone {
      width: min(28vw, 250px);
      min-width: 185px;
      aspect-ratio: 250 / 510;
      background: #0e0e0e;
      border-radius: var(--radius-shell);
      padding: 6px;
      position: relative;
      box-shadow: var(--shadow);
      border: 1px solid rgba(255,255,255,0.12);
      flex: 0 0 auto;
      transition: transform 0.35s ease, opacity 0.35s ease;
    }

    .phone--center { transform: translateY(-22px) scale(1.05); }

    /* ── Screen ── */
    .phone__screen {
      background: var(--surface);
      border-radius: calc(var(--radius-shell) - 6px);
      overflow: hidden;
      height: 100%;
      display: flex;
      flex-direction: column;
      position: relative;
    }

    /* ── Status bar ── */
    .statusbar {
      position: absolute;
      top: 10px; left: 14px; right: 14px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 4;
      color: white;
      font-size: 8px;
      font-weight: 600;
      letter-spacing: 0.02em;
      pointer-events: none;
    }

    /* ── Dynamic island / notch ── */
    .notch {
      position: absolute;
      top: 8px; left: 50%;
      transform: translateX(-50%);
      width: 72px; height: 16px;
      border-radius: 999px;
      background: #080808;
      z-index: 5;
    }

    /* ── Hero image area ── */
    .hero {
      height: 46%;
      position: relative;
      border-bottom-left-radius: 22px;
      border-bottom-right-radius: 22px;
      overflow: hidden;
    }

    .hero img {
      width: 100%; height: 100%;
      object-fit: cover;
      display: block;
    }

    /* ── Text content ── */
    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 18px 20px 16px;
    }

    .copy { text-align: center; padding-top: 8px; }

    .copy h2 {
      margin: 0;
      font-size: 13px;
      font-weight: 800;
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }

    .copy p {
      margin: 10px auto 0;
      max-width: 22ch;
      font-size: 8.5px;
      line-height: 1.45;
      color: var(--muted);
    }

    /* ── Footer row: Skip | Dots | Next ── */
    .footer {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding-top: 12px;
    }

    .skip {
      border: 0; background: transparent;
      color: var(--text);
      font-size: 8.5px; padding: 0;
      cursor: pointer;
    }

    .dots { display: flex; gap: 5px; }

    .dot {
      width: 6px; height: 6px;
      border-radius: 999px;
      background: #e5e7eb;
      transition: width 0.25s ease, background 0.25s ease;
    }

    .dot.active { width: 16px; background: var(--accent); }

    .next {
      width: 22px; height: 22px;
      border-radius: 999px; border: 0;
      background: var(--accent);
      color: white;
      display: grid; place-items: center;
      font-size: 12px; font-weight: 700;
      cursor: pointer;
      box-shadow: 0 6px 14px rgba(255,107,99,0.35);
    }

    /* ── Home indicator bar ── */
    .home-indicator {
      position: absolute;
      left: 50%; bottom: 8px;
      transform: translateX(-50%);
      width: 72px; height: 3px;
      border-radius: 999px;
      background: #111; opacity: 0.9;
    }

    /* ── Bottom controls (arrange buttons) ── */
    .controls {
      position: fixed;
      left: 50%; bottom: 18px;
      transform: translateX(-50%);
      display: flex; gap: 10px;
      background: rgba(255,255,255,0.72);
      backdro...