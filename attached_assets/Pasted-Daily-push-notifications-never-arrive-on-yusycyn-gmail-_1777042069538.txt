Daily push notifications never arrive on yusycyn@gmail.com's device. A direct check of the production database shows the real reason: the onesignal_player_id column on yusycyn's user_profiles row is empty, so the scheduler's WHERE onesignal_player_id IS NOT NULL filter excludes that account from every send. The "Sending / 0 / N/A" rows visible in the OneSignal dashboard target the other registered devices (e.g. 357c7ff8-… belongs to yellowfoodfood@gmail.com), not yusycyn — yusycyn was never in the recipient list to begin with.

Production today (2026-04-24) has only 3 user_profiles rows with a onesignal_player_id set: yellowfoodfood@gmail.com, yellowwmeowwww@gmail.com, yuszeyui@connect.hku.hk. yusycyn@gmail.com is signed in (user id cee83e6f-0ae6-402d-a973-bc46c64a19b4, onboarding complete) but their POST /api/onesignal/register either never fires, fires before the wrapper has a player id to give, or fires and stores something that later gets cleared. There is no UI surface today that tells the developer "this device is/isn't registered with OneSignal" without manually querying the DB — every diagnosis to date has been a guess.

A secondary issue that is real but separate: when the deployed server wakes up after the user's local trigger time has passed, delivery_time_of_day + delayed_option: "timezone" rolls delivery to the next day's occurrence. Today's deployment didn't wake until 14:16 UTC = 22:16 HKT, missing the 6 PM HK re-engagement window structurally and the 10 PM HK check-in window by 16 minutes. This affects the other registered devices, not yusycyn, but should be fixed in the same pass since it touches the same files and the same dev-panel.

A third, unrelated-but-confusing issue: the DEV_EMAILS block in server/routes.ts carries a stale comment claiming yusycyn = 352049ea-0f08-4ca5-a980-62bef203e2a3, but the actual production user id is cee83e6f-0ae6-402d-a973-bc46c64a19b4. That comment misled diagnosis and should be corrected.

Done looks like
yusycyn@gmail.com on the TestFlight device has a non-null onesignal_player_id stored on their user_profiles row in production, and receives the 10 PM daily check-in the same evening on a normal run.
The dev panel shows, at a glance for the currently logged-in user: their email, their stored onesignal_player_id (or a clear "not registered" state), the timestamp of their last successful /api/onesignal/register call, and the source the wrapper used to obtain the player id (NativelyNotifications / NativelyPush / global OneSignal / none).
A "Re-register OneSignal now" button in the dev panel forces the client to re-attempt player-id retrieval and registration, and shows the result inline (success + new player id, or which lookup paths failed).
Server logs for every /api/onesignal/register call include the user's email and the registered player id (truncated for privacy if needed), so we can confirm registration end-to-end without a DB query.
For users that are registered, scheduled sends no longer silently roll into "tomorrow" when today's local trigger has already passed — they fire today instead.
The OneSignal dashboard for these scheduled sends transitions out of "Sending / 0 / N/A" within minutes of the local trigger, and the server logs the per-notification delivery report (recipients / successful / failed) once that trigger has fired.
The stale yusycyn user-id comment in server/routes.ts is corrected (or removed) so future debugging doesn't go down the same wrong path.
Out of scope
Adding per-user notification time preferences.
Changing notification copy, deep links, or which audience each reminder targets.
Switching off OneSignal or moving to a different push provider.
Building a full admin UI for browsing past notification deliveries.
Re-architecting the dedup file into the database.
Steps
Diagnose why yusycyn never registered. Read the client-side OneSignal player-id lookup flow in client/src/App.tsx and confirm what it does when none of the wrapper paths (NativelyNotifications, NativelyPush, global OneSignal) returns a player id on the first attempt — does it retry, give up silently, or log? Add a small structured client log for each attempt that names the path tried and the outcome, and have the client POST /api/dev/onesignal-bridge-probe (dev-gated) with the result so it shows up in server logs for the actual problem device. Use the probe output to confirm whether yusycyn's TestFlight install is exposing any player-id-bearing bridge at all.

Surface registration state in the dev panel. Add a "Push registration" card to client/src/pages/dev-panel.tsx that shows, for the currently logged-in user: email, stored onesignal_player_id (full value, with a copy button), timestamp of last successful register call, and the most recent bridge-probe result from step 1. Include a "Re-register OneSignal now" button that re-runs the client lookup + /api/onesignal/register flow and shows the response inline. Add a tiny new GET endpoint (dev-gated) that returns the current user's registration status so the panel can display it.

Make /api/onesignal/register self-auditing. In server/routes.ts, when this endpoint accepts a player id, also log a single line of the form onesignal/register email=<email> player_id=<full id> previous=<prev id or none>, and store a onesignal_registered_at timestamp on the user_profiles row alongside onesignal_player_id. Add the column to shared/schema.ts and run the schema sync. The dev panel card from step 2 reads this timestamp directly.

Correct the stale dev-id comment. In server/routes.ts near the DEV_EMAILS block, replace the comment that claims yusycyn = 352049ea-0f08-4ca5-a980-62bef203e2a3 with the real production user id cee83e6f-0ae6-402d-a973-bc46c64a19b4, or remove the hardcoded id entirely if it is no longer used by any code path.

Stop scheduled sends from silently rolling to tomorrow. In each of the four schedulers (daily check-in, Sunday planning, late dinner, re-engagement), before calling OneSignal with delivery_time_of_day + timezone, split the targeted player IDs into two groups using each user's most recently observed device timezone (captured at registration — see step 6): users for whom today's local trigger time is still in the future versus already passed. Send the future group via the existing timezone path; send the passed group either immediately (no schedule) or with an explicit send_after set to the next occurrence — whichever is the smaller change in server/onesignal.ts. Log both groups separately so the logs explain why each user got the timing they got.

Capture and store the device timezone on registration. Extend POST /api/onesignal/register to accept the device IANA timezone (e.g. Asia/Hong_Kong) alongside the player id, sent by the client from Intl.DateTimeFormat().resolvedOptions().timeZone. Store it on the user_profiles row. When the timezone is unknown, fall back to UTC and treat the trigger as "may have passed" so it fires immediately rather than silently rolling.

Move the daily scheduling pass earlier in the UTC day. Lower SCHEDULE_HOUR_UTC so the daily pass runs early enough that every timezone still has all of today's local trigger times in the future (00:00 UTC is the simplest correct choice). On top of the existing setInterval, add a mechanism that guarantees the deployment is awake to run that pass — either by switching this deployment to an always-on configuration, or by registering an external uptime ping that hits a lightweight endpoint shortly before the scheduling hour. Pick whichever fits the current deployment with the smallest change and document the choice in a short comment in server/notifications.ts.

Make scheduled sends auditable by email. In each scheduling function, when building the targeted-player-id list, also collect the corresponding user email and log a single line of the form: daily_check_in: targeted N users emails=[a@x, b@y, ...] schedule=10:00PM/timezone. Truncate gracefully for very large lists. Pair this with the existing OneSignal request body log in server/onesignal.ts so the two can be joined by timestamp.

Add a delivery-report read after the local trigger. Today the code reads the OneSignal delivery report at t+0 and once at t+6s, which is too early for a delivery_time_of_day send. Add one additional read scheduled for shortly after the configured local trigger time and log the same recipients/successful/failed counts, so the logs confirm the send actually completed.

Verify against production. After deploy, on a Hong Kong device logged in as yusycyn@gmail.com: open the new dev-panel card, hit "Re-register OneSignal now", confirm the onesignal_player_id is non-null in production, then trigger the existing dev "test daily check-in" send and confirm it arrives on the device immediately. Then before the next 10 PM HKT, confirm the natural daily run also arrives. Capture the relevant log lines (registration line, targeted emails, notification id, delivery report) in the task completion notes.